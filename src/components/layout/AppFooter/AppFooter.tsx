export const AppFooter = () => (
  <footer className="mt-12 py-6 text-center text-xs text-gray-500 border-t border-gray-200 max-w-7xl mx-auto px-4">
    <p>
      CESI Helpdesk — service de signalement des incidents du campus.
      {' '}
      Une urgence immédiate (fumée, blessure, danger électrique) doit être signalée par téléphone,
      pas par ce formulaire.
    </p>
  </footer>
)
