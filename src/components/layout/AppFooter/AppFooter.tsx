export const AppFooter = () => (
  <footer className="mt-12 py-6 text-center text-xs text-gray-400 border-t border-gray-200 max-w-7xl mx-auto">
    <p>Document d'Architecture : App codée en React (State Management local). Pour un déploiement réel, relier `tickets` à une base de données (ex: Firebase/PostgreSQL). Le champ photo est simulé et prêt pour intégrer une compression canvas. Couleurs alignées sur la charte CESI.</p>
  </footer>
)
